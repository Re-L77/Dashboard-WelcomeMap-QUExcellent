FROM nginx:alpine
# nginx config is inside the docker/ folder; copy it using path relative to build context (project root)
COPY docker/nginx-dashboard.conf /etc/nginx/conf.d/default.conf
# Copy views and static into nginx html folder during build
COPY app/views /usr/share/nginx/html/
COPY app/static /usr/share/nginx/html/static/
